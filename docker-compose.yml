services:
  mysql:
    image: mysql:8.0
    container_name: content_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wp_user
      MYSQL_PASSWORD: wp_password
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - content_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 5

  wordpress:
    build:
      context: ./wordpress
      dockerfile: Dockerfile
    container_name: content_wordpress
    restart: unless-stopped
    depends_on:
      - mysql
    working_dir: /var/www
    volumes:
      - ./wordpress:/var/www
    networks:
      - content_network

  nextjs:
    build:
      context: ./nextjs
      dockerfile: Dockerfile
    container_name: content_nextjs
    restart: unless-stopped
    ports:
    - "3000:3000"
    depends_on:
      - wordpress
    networks:
      - content_network

  nginx:
    image: nginx:latest
    container_name: content_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./wordpress/web:/var/www/web
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - wordpress
      - nextjs
    networks:
      - content_network

volumes:
  mysql_data:

networks:
  content_network:
